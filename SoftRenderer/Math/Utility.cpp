#include "Utility.h"
namespace sbm
{
	float sin(float rad)
	{
		return std::sin(rad);
	}

	float cos(float rad)
	{
		return std::cos(rad);
	}

	float pow(float left, float right)
	{
		return std::powf(left, right);
	}
}